/*
** © 2013 by Philipp Dunkel <p.dunkel@me.com>. Licensed under MIT License.
*/

module.exports = compile;

function compile(context, file, options, callback) {
  var target = file.path.replace(new RegExp(options.search), options.replace);
  console.log(file.name+' => '+target);
  //TODO: Actually do the compiling
  process.nextTick(callback);
}
